name: CI - Upload Binaries

on:
  push:
  workflow_dispatch:
  release:
    types: [published]

concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}

jobs:
  build_mojave:
    name: Zip, Sign, and Upload Binaries
    runs-on: macos-latest

    steps:
      # 1️⃣ Checkout repository
      - uses: actions/checkout@v5

      # 2️⃣ Import code signing certificates
      - name: Import code signing certificates
        uses: apple-actions/import-codesign-certs@v3
        with:
          p12-file-base64: ${{ secrets.SIGNING }}
          p12-password: ${{ secrets.SECRETSPASS }}

      # 3️⃣ Prepare keychain
      - name: Prepare keychain
        run: |
          security list-keychains -s signing_temp.keychain login.keychain
          security unlock-keychain -p ${{ secrets.SECRETSPASS }} signing_temp.keychain
          security default-keychain -s signing_temp.keychain
          security find-identity -v -p codesigning

      # 4️⃣ Clean, strip, and sign binaries
      - name: Clean, strip, and sign binaries
        run: python3 ci.py

      # 5️⃣ Tag folder as signed
      - name: Tag folder
        run: touch Universal-Binaries/.signed

      # 6️⃣ Build and Sign DMG (без явного identity)
      - name: Build DMG
        run: |
          # Разблокируем keychain
          security unlock-keychain -p ${{ secrets.SECRETSPASS }} signing_temp.keychain
          security default-keychain -s signing_temp.keychain

          # Список доступных identity для отладки
          echo "Available signing identities:"
          security find-identity -v -p codesigning

          # Генерация DMG с подписью (скрипт сам берёт первый доступный сертификат)
          python3 Generate-DMG.command --sign || echo "DMG signing failed, continuing..."

          # Проверка подписи
          if [ -f Universal-Binaries.dmg ]; then
            codesign -dv --verbose=4 "Universal-Binaries.dmg"
          else
            echo "DMG не сгенерирован"
          fi

      # 7️⃣ Generate checksum
      - name: Generate Checksums
        run: shasum -a 256 Universal-Binaries.dmg > sha256sum.txt || echo "No DMG generated"

      # 8️⃣ Upload artifacts to Actions
      - name: Upload Universal Binaries to Artifacts
        uses: actions/upload-artifact@v5
        with:
          name: Universal Binaries
          path: Universal-Binaries.dmg

      - name: Upload Checksums to Artifacts
        uses: actions/upload-artifact@v5
        with:
          name: sha256sum.txt
          path: sha256sum.txt

      # 9️⃣ Prepare files for release
      - name: Prepare release files
        run: |
          mkdir -p deploy
          mv *.dmg sha256sum.txt deploy/ || echo "No files to move"
          ls -la deploy

      # 🔟 Upload to GitHub Release (только при событии release)
      - name: Upload to Release
        if: github.event_name == 'release'
        uses: svenstaro/upload-release-action@v2
        with:
          file: deploy/*
          file_glob: true
          token: ${{ secrets.PAT_TOKEN }}
